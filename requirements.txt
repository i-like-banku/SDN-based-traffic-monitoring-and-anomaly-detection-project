# SDN Traffic-Monitoring / Anomaly-Detection controller dependencies
#
# On a classic Ryu server (Ubuntu 20.04, Python 3.8/3.9) install:
#     ryu==4.34
#     eventlet==0.30.2
#     setuptools<58
#
# The controller code also runs unmodified on the maintained os-ken fork,
# which is the recommended choice on newer systems where the classic ryu
# package no longer builds (Python 3.10+). To use os-ken instead:
#     os-ken>=2.7
#     eventlet
#
# Only ONE of ryu / os-ken is needed. The application detects which is present.

ryu==4.34 ; python_version < "3.10"
os-ken>=2.7 ; python_version >= "3.10"
eventlet
# used only by analysis/plot_stats.py and analysis/make_diagrams.py (optional)
matplotlib
