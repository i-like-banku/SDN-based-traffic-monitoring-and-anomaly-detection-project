module.exports = function (H) {
  const { h1, h2, h3, p, bullet, num, figure, table, pageBreak, spacer } = H;
  return [
    h1("CHAPTER TWO: LITERATURE REVIEW"),

    h2("2.1 Introduction"),
    p("This chapter reviews the existing body of knowledge that this project draws upon. The goal is to understand what is already known and built in the areas the project touches, so that the design rests on solid foundations and so that the project\u2019s own contribution is clear. The review moves from the general to the specific. It begins with Software-Defined Networking itself, what it is, how it is structured, and why it matters. It then looks at the OpenFlow protocol that makes SDN work in practice, and at the main controllers that researchers and industry use. After that it turns to the specific area of this project: monitoring network traffic and detecting anomalies. Finally, it reviews several systems that are similar in spirit to this one, weighing their strengths and weaknesses, and it identifies the gap that this project sets out to fill."),
    p("The sources consulted include peer-reviewed journal articles and conference papers, which are the primary and most authoritative sources in this field, along with technical documentation for the tools used. Where earlier researchers\u2019 ideas are drawn upon, they are credited, and the review aims to present an up-to-date picture of a fast-moving field."),

    h2("2.2 Software-Defined Networking"),
    p("Software-Defined Networking is best understood as a new way of organising a network rather than a single product. In a traditional network, every switch and router is a self-contained unit that both decides where traffic should go and physically moves it. These two jobs, deciding and moving, are bundled together inside each device. SDN\u2019s central insight is that these two jobs can and should be separated. The deciding is pulled out into a central controller, and the devices are left to do only the moving, following instructions the controller gives them."),
    p("Researchers describe this separation in terms of planes. The control plane is the set of functions that decide how traffic should be handled. The data plane, sometimes called the forwarding plane, is the set of functions that actually forward packets. In traditional networking these planes are combined in every device; in SDN the control plane is centralised in the controller while the data plane stays distributed across the switches. A third plane, the application plane, sits above the controller and holds the programs, like this project\u2019s monitoring system, that tell the controller what the network should do."),

    h3("2.2.1 The Architecture of Software-Defined Networking"),
    p("The SDN architecture is usually drawn as three layers stacked on top of one another. At the bottom is the infrastructure layer, made up of the physical or virtual switches that forward traffic. In the middle is the control layer, which is the controller itself. At the top is the application layer, which contains the network applications that define how the network should behave."),
    p("These layers talk to each other through two interfaces. The link between the controller and the switches below it is called the southbound interface, and OpenFlow is the most common protocol used for it. The link between the controller and the applications above it is called the northbound interface, and it is usually a programming interface (an API) that applications use to tell the controller what they want. This project\u2019s monitoring system sits at the application layer and uses the controller\u2019s facilities to collect statistics from the switches through the southbound OpenFlow interface."),
    p("The great advantage of this layered design is that it makes the network programmable. Because the controller has a complete view and can be extended with software, new network behaviour, including security monitoring like this project\u2019s, can be added by writing a program rather than by replacing hardware or reconfiguring dozens of devices by hand."),

    h3("2.2.2 The Benefits of Software-Defined Networking"),
    p("The literature consistently highlights several benefits of SDN. The first is centralised control: because one controller sees and manages the whole network, administrators gain a single point from which to observe and direct everything, which greatly simplifies management. The second is programmability: network behaviour can be changed through software, making the network flexible and quick to adapt. The third is a global view: the controller\u2019s complete picture of the network allows smarter decisions than any single device could make on its own. The fourth is easier innovation: researchers and vendors can try out new ideas by writing controller applications, without waiting for new hardware."),
    p("For a security-monitoring project like this one, the global view and the programmability are especially valuable. The global view means the whole network can be watched from one place, and the programmability means detection and response can be added simply by writing an application for the controller."),

    h3("2.2.3 The Challenges of Software-Defined Networking"),
    p("The literature is equally honest about the challenges SDN brings. The most discussed is the risk that the controller becomes a single point of failure or a bottleneck: because so much depends on it, if it goes down or becomes overloaded, the whole network can suffer. Researchers address this with techniques like running several controllers together for resilience. A second challenge is security of the controller itself: since it is the brain of the network, it is an attractive target, and protecting it is essential. A third is scalability: a single controller must be able to keep up with a large and busy network. A fourth, often raised in practice, is the difficulty of migrating from traditional networks to SDN, since organisations already own a great deal of traditional equipment and cannot replace it all at once."),
    p("This project keeps these challenges in mind. By making its detection engine lightweight and efficient, it avoids overloading the controller, and by focusing on a clear, well-understood set of attacks it keeps the controller\u2019s security workload manageable."),

    h2("2.3 The OpenFlow Protocol"),
    p("If SDN is the idea of separating control from forwarding, OpenFlow is the practical language that makes the separation work. It is the protocol that the controller uses to talk to the switches beneath it. Understanding OpenFlow is essential to this project, because the whole monitoring system is built on the statistics that OpenFlow makes available."),

    h3("2.3.1 The Role of OpenFlow in SDN"),
    p("OpenFlow gives the controller two main powers over each switch. First, it lets the controller install flow rules, instructions that tell the switch what to do with packets that match certain patterns, such as \u201Csend packets going to this address out of port 3\u201D or \u201Cdrop all packets from this source.\u201D Second, it lets the controller read statistics from the switch, such as how many packets and bytes each flow rule has matched. This project uses both powers: it reads statistics to monitor traffic, and it installs rules to forward normal traffic and to block attackers."),
    p("Each OpenFlow switch keeps its forwarding rules in one or more flow tables. When a packet arrives, the switch looks for a matching rule and follows its instructions. If no rule matches, the switch can send the packet up to the controller to ask what to do, an event known as a packet-in. The controller can then decide how to handle it and install a rule so that future similar packets are handled automatically by the switch, without troubling the controller again."),

    h3("2.3.2 How OpenFlow Communication Works"),
    p("Communication between the controller and a switch begins with a handshake, in which the two agree on the version of OpenFlow they will use and the switch tells the controller about its capabilities. After that, the controller can install rules, and the switch can send packet-in messages when it meets traffic it does not know how to handle. Crucially for this project, the controller can at any time send a statistics request, asking the switch to report the counters for its flows or its ports, and the switch replies with the current numbers."),
    p("This project relies on that statistics facility. Every few seconds the controller asks each switch for its flow and port statistics. By comparing the new numbers with the previous ones, the controller can work out how fast traffic is flowing right now, for example, how many packets per second a particular flow is carrying. These per-second rates are the raw material that the detection engine studies."),

    h3("2.3.3 Advantages and Challenges of OpenFlow"),
    p("OpenFlow\u2019s main advantage is that it is an open, widely supported standard, so controllers and switches from different sources can work together. It gives fine-grained control over traffic and exposes exactly the kind of statistics that monitoring needs. Its challenges include the overhead of communication between controller and switches when traffic is heavy, and the limited size of the flow tables in some switches, which means rules must be used carefully. This project keeps its use of the switches efficient by installing normal forwarding rules that let ordinary traffic be handled in hardware, reserving the controller\u2019s attention for statistics-gathering and for reacting to attacks."),

    h2("2.4 SDN Controllers"),
    p("The controller is the heart of an SDN network, and several mature controllers exist. The choice of controller shapes how an SDN application is built, so it is worth comparing the main options that the literature discusses."),

    h3("2.4.1 OpenDaylight"),
    p("OpenDaylight is one of the best-known open-source controllers, developed under the Linux Foundation with a large community behind it. It has a modular design that can be extended for many uses and supports OpenFlow along with several other protocols. Researchers value it for its flexibility and strong community support. Its drawbacks are that it is complex to set up, demands considerable technical skill, and can use a lot of computing resources when deployed at scale. This weight makes it more than is needed for a focused project like this one."),

    h3("2.4.2 ONOS (Open Network Operating System)"),
    p("ONOS is a controller designed for large, demanding networks such as those run by telecommunications companies. Its strength is a distributed design that spreads control across several machines, making it resilient and highly scalable. The literature recommends it where reliability and the ability to handle huge amounts of traffic are critical. Its drawbacks are that it is complex to deploy and has a steep learning curve, again making it heavier than necessary for this project\u2019s goals."),

    h3("2.4.3 Ryu"),
    p("Ryu is a lightweight, Python-based SDN framework that is popular for research and education. It is straightforward to learn, integrates smoothly with Python, and supports OpenFlow well. Because it is written in Python, it is easy to extend with custom logic, exactly what a detection engine needs. Ryu is component-based: an application is written as one or more small programs that respond to network events, such as a switch connecting or a statistics reply arriving. This project uses Ryu because its simplicity and Python foundation make it ideal for building and clearly explaining a monitoring application, and because it is widely used in the research this project builds on."),
    p("It is worth noting a practical point discovered during this project. The classic Ryu package has become difficult to install on the newest versions of Python because of changes in Python\u2019s packaging tools. A maintained fork called os-ken, which keeps the same programming interface, solves this. The system in this project is written so that it runs on either, which keeps it usable both on older, stable systems and on the newest ones."),

    h3("2.4.4 Comparison of the Controllers"),
    table([
      ["Controller", "Language", "Best suited to", "Main drawback"],
      ["OpenDaylight", "Java", "Flexible enterprise use", "Complex, resource-heavy"],
      ["ONOS", "Java", "Large carrier networks", "Steep learning curve"],
      ["Ryu", "Python", "Research and education", "Not aimed at huge scale"],
    ]),
    H.caption("Table 2.1: Comparison of the three main SDN controllers."),
    p("For this project, Ryu is the clear choice. The project does not need the industrial scale of ONOS or the sprawling flexibility of OpenDaylight; it needs a controller that is easy to program with custom detection logic and easy to explain. Ryu fits that need exactly."),

    h2("2.5 Network Monitoring and Anomaly Detection"),
    p("Having covered the SDN foundations, the review now turns to the specific subject of this project: watching network traffic and detecting when it looks abnormal."),
    p("Network monitoring means keeping track of what traffic is flowing through a network so that administrators can understand its health, performance, and security. Traditionally this was done with dedicated hardware appliances or with software that collected traffic samples from routers. These approaches work, but they add cost and complexity, and they usually give only a partial view. SDN changes this by making rich statistics available from every switch through the controller, so monitoring can be done centrally and cheaply."),
    p("Anomaly detection is the practice of learning what normal traffic looks like and flagging traffic that departs sharply from it. The literature divides intrusion detection broadly into two families. Signature-based detection matches traffic against a library of known attack patterns; it is precise for known attacks but cannot catch new ones. Anomaly-based detection, by contrast, models normal behaviour and raises an alarm when behaviour strays far from the model; it can catch previously unseen attacks but may sometimes flag unusual-but-harmless traffic. Because SDN provides a continuous stream of network-wide statistics, it is a natural fit for anomaly-based detection, and much recent research explores exactly this combination."),
    p("Researchers have applied a spectrum of techniques to SDN-based anomaly detection, from simple statistical thresholds through to complex machine-learning models. Machine-learning approaches can be powerful, learning subtle patterns that simple rules would miss, but they bring costs: they need training data, they are harder to understand and trust, and they add computational weight to the controller. Statistical-threshold approaches, on the other hand, are lightweight, fast, easy to explain, and easy to tune. For a project whose goals are a practical, working, understandable system that runs comfortably inside the controller, the statistical approach is well matched, and it is the approach this project takes. It is worth adding that the two approaches are not rivals so much as points on a scale; a statistical system like this one can serve as a solid, transparent foundation that a machine-learning layer could later be added to."),
    p("The specific attacks this project targets are among the most common and well studied. A port scan is a reconnaissance technique in which an attacker probes many ports or many hosts to discover which services are running and might be vulnerable; its signature is one source touching a great many destinations with only a trickle of traffic to each. A flood or denial-of-service attack tries to overwhelm a target by sending traffic far faster than it can handle; its signature is an unusually high packet rate from a source. A volume anomaly is any sharp, unexplained jump in the amount of traffic a source sends, which can indicate data theft, a compromised machine, or the early stage of an attack. Each of these leaves a clear fingerprint in the flow statistics that OpenFlow provides, which is what makes them good targets for an SDN-based detector."),

    h2("2.6 Review of Similar Systems"),
    p("Several researchers and projects have built systems that, like this one, use SDN for monitoring and security. Reviewing a few of them shows what has been achieved, what is still missing, and where this project fits."),

    h3("2.6.1 System One: SDN-Based DDoS Detection Using Flow Statistics"),
    p("A number of studies have built systems that detect distributed denial-of-service attacks by collecting flow statistics through an SDN controller and watching for sudden surges in traffic. These systems typically poll the switches at regular intervals, compute traffic rates, and raise an alert when a rate crosses a threshold. Their strength is that they demonstrate clearly that flow statistics gathered through the controller are enough to catch flooding attacks quickly and cheaply, without extra hardware. Their main limitation is that many of them focus narrowly on flooding alone and stop at raising an alert, leaving the actual response to a human. This project builds on their proven statistics-polling approach but broadens the range of attacks detected and adds automatic response."),

    h3("2.6.2 System Two: Machine-Learning Intrusion Detection in SDN"),
    p("Other researchers have applied machine-learning models to traffic collected through an SDN controller, training classifiers to distinguish attack traffic from normal traffic. These systems can be impressively accurate and can recognise a wide range of attack types, including subtle ones. Their strength is detection power. Their drawbacks are the practical costs already noted: they need labelled training data, they are harder to understand and to trust because their decisions are not easily explained, and they place a heavier load on the controller. For a project that values a working, transparent, and lightweight system, these costs are significant. This project deliberately chooses explainable statistical detection instead, while acknowledging that a machine-learning layer would be a natural future extension."),

    h3("2.6.3 System Three: SDN Firewalls and Automatic Mitigation"),
    p("A third strand of work has explored using the controller\u2019s power to install rules as a way of building firewalls and automatic defences directly in the network. These systems show that, once an attacker is identified, the controller can block them across the whole network in a single coordinated move by installing drop rules on the switches. Their strength is fast, network-wide response. Their limitation is that they often assume the attacker has already been identified by some other means, and concentrate on the blocking rather than the detection. This project joins the detection and the response together: it both identifies attackers through its detection engine and blocks them automatically through the controller."),

    h2("2.7 Summary and Research Gap"),
    p("Taken together, the reviewed work establishes several things clearly. Software-Defined Networking, through the OpenFlow protocol and a controller such as Ryu, provides a central, statistics-rich view of the network that is ideally suited to traffic monitoring. Flow statistics gathered through the controller are enough to detect common attacks like flooding. The controller\u2019s power to install rules makes fast, network-wide automatic response possible. And anomaly-based detection, whether by statistics or machine learning, is a good fit for the continuous data that SDN provides."),
    p("What is less common is a single, complete, and clearly explained system that ties all of these threads together: one that continuously monitors the whole network, detects several different classes of attack, not just flooding, using a lightweight and explainable engine, responds automatically by blocking attackers, and presents everything through a live dashboard, all in a form that can actually be run and tested. Many existing systems do one or two of these things well but leave the others out, or they achieve high detection power at the cost of transparency and simplicity."),
    p("This is the gap the project fills. It combines network-wide statistics monitoring, multi-class explainable anomaly detection, automatic mitigation, and live visualisation into one working system built on Ryu and tested on Mininet. The next chapter sets out the analysis and design of that system in detail."),
    pageBreak(),
  ];
};
