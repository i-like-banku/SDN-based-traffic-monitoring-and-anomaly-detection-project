module.exports = function (H) {
  const { h1, h2, h3, p, bullet, num, figure, pageBreak, spacer } = H;
  return [
    h1("CHAPTER ONE: INTRODUCTION"),

    h2("1.1 Introduction"),
    p("A computer network is simply a collection of devices such as computers, phones, servers, routers, and switches that are connected together so they can share information. Almost everything we do online, from sending a message to streaming a video to paying a bill, depends on networks quietly moving data from one place to another. As more of daily life and business has moved online, these networks have grown larger and more complicated, and keeping them running smoothly and safely has become a serious challenge."),
    p("For a long time, networks were built in a way that gave each device its own brain. Every router and switch decided on its own how to move traffic, based on settings that a network administrator had to type into it by hand. This worked when networks were small, but it has two big weaknesses. First, there is no single place where an administrator can see the whole network at once, which makes it hard to understand what is really happening across all the devices. Second, because each device works on its own, reacting to a problem, such as an attack or a sudden traffic jam, means logging into many devices one at a time, which is slow and error-prone."),
    p("Software-Defined Networking, usually shortened to SDN, was created to solve these problems. The core idea of SDN is to split the network into two parts. The first part, called the control plane, is the decision-making brain that works out where traffic should go. The second part, called the data plane, is the muscle that actually forwards the traffic from one port to another. In a traditional network these two parts live together inside every device. In SDN they are pulled apart: the forwarding muscle stays in the switches, but the decision-making brain is lifted out and placed in a single piece of software called the controller. The controller has a complete, bird\u2019s-eye view of the whole network and can program every switch from one place."),
    p("This project takes advantage of that bird\u2019s-eye view to do something useful for network security: watch the traffic flowing through the network and automatically spot when something looks wrong. The system built in this project is a traffic-monitoring and anomaly-detection tool that lives inside the SDN controller. It constantly gathers statistics about the traffic passing through the switches, studies those statistics for patterns that match known kinds of attack, and raises an alert, and can even block the attacker automatically, the moment it sees trouble."),
    p("Before going further, it helps to define a few key terms that appear throughout this report:"),
    bullet("Software-Defined Networking (SDN): an approach to building networks that separates the control logic (deciding where traffic goes) from the forwarding hardware, and centralises the control logic in a software controller."),
    bullet("Controller: the central software brain of an SDN network. It has an overall view of the network and installs the rules that tell each switch how to handle traffic. In this project the controller runs the Ryu SDN framework."),
    bullet("OpenFlow: the standard language, or protocol, that the controller uses to talk to the switches. It lets the controller ask switches for statistics and install forwarding or blocking rules."),
    bullet("Flow: a stream of related packets travelling between two points. An example would be all the packets of a single web download. Switches keep counters for each flow, such as how many packets and bytes it has carried."),
    bullet("Anomaly detection: the practice of learning what normal behaviour looks like and then flagging behaviour that departs sharply from it. In networking, an anomaly might be a machine suddenly contacting hundreds of ports, or sending traffic far faster than usual."),
    bullet("Intrusion Detection System (IDS): a tool that watches a network or computer for signs of malicious activity and warns administrators when it finds them. The system in this project is a lightweight IDS built into the SDN controller."),
    bullet("Mininet: a program that creates a realistic virtual network of switches and hosts on a single computer, so that SDN software can be built and tested without needing real hardware."),
    p("With these ideas in place, the rest of this chapter explains the background to the project, the specific problem it sets out to solve, the questions it aims to answer, its goals and boundaries, the approach used to build it, and how the rest of the report is organised."),

    h2("1.2 Background of the Study"),
    p("To understand why a system like this is needed, it helps to look at how networks have changed and why the traditional way of managing them is struggling to keep up. Twenty years ago, a typical organisation had a fairly small and stable network. The number of devices was manageable, traffic patterns were predictable, and threats were fewer and simpler. In that world, configuring each device by hand and checking on them occasionally was good enough."),
    p("That world has changed completely. Cloud computing means that services now run in large data centres where thousands of virtual machines are created and destroyed every hour. Mobile devices, laptops, and Internet-of-Things gadgets connect and disconnect constantly. Video, voice, and real-time applications demand steady, high-quality connections. All of this has pushed the amount of traffic and the number of devices to levels that would have been unimaginable before, and it has made traffic patterns far less predictable."),
    p("At the same time, the threats facing networks have grown sharply in both number and sophistication. Attackers routinely scan networks looking for weak points, flood servers with junk traffic to knock them offline, and try to sneak malicious traffic past defences. Because attacks now unfold in seconds, the slow, manual, device-by-device style of network management leaves dangerous gaps. By the time an administrator notices something is wrong on one device and starts checking the others, an attacker may already have done serious damage."),
    p("SDN offers a way out of this bind. Because the controller sees the whole network and can program every switch instantly, it becomes possible to monitor traffic everywhere at once and to react to problems in one coordinated move rather than many scattered ones. Researchers quickly realised that this central vantage point is ideal not just for managing traffic but for spotting attacks. Instead of installing separate, expensive monitoring appliances at various points in the network, the monitoring can be built right into the controller, which already receives a steady stream of information from every switch."),
    p("The OpenFlow protocol makes this practical. Every OpenFlow switch keeps counters for the flows passing through it, such as how many packets and bytes each flow has carried, how long it has existed, and so on. The controller can ask any switch for these counters at any time. By collecting them regularly and studying how they change, the controller can build an accurate picture of what the network is doing and notice when that picture starts to look abnormal. This project is built squarely on that idea: it uses the statistics that OpenFlow switches already collect, gathered through the Ryu controller, to detect attacks as they happen."),
    p("There are broadly two ways to detect attacks. The first, called signature-based detection, keeps a library of known attack patterns and matches traffic against them. It is accurate for attacks it already knows about but blind to new ones. The second, called anomaly-based detection, learns what normal traffic looks like and flags anything that deviates strongly from it. It can catch attacks it has never seen before, at the cost of occasionally raising a false alarm on unusual but harmless traffic. This project uses anomaly-based detection, because the central, statistics-rich view provided by SDN is especially well suited to it, and because the goal is to catch broad classes of attack, scanning, flooding, and abnormal traffic volumes, rather than one specific known threat."),

    h2("1.3 Problem Statement"),
    p("Networks today face constant probing and attack, but the traditional way of watching for and responding to these threats has not kept pace. The heart of the problem is a lack of a single, complete, real-time view of what is happening across the whole network, combined with the slowness of responding to threats one device at a time."),
    p("In a traditional network, monitoring is fragmented. Each device knows only about the traffic passing through it, and stitching together a picture of the whole network means collecting and combining data from many separate sources, often after the fact. This makes it hard to spot attacks that show themselves only when you look across the whole network, for example, a single machine quietly scanning many different servers, touching each one only briefly. Any one server sees just a little unusual traffic, but from a network-wide view the scanning pattern is obvious. Without that view, such attacks slip through."),
    p("Responding is just as fragmented. Even when an attack is finally noticed, stopping it usually means an administrator manually logging into affected devices and changing their settings, one by one. During the minutes or hours this takes, the attack continues. For fast-moving attacks like floods, this delay can be the difference between a minor incident and a serious outage."),
    p("SDN removes the excuse for both of these weaknesses, because the controller already has the network-wide view and the power to reprogram every switch instantly. Yet many networks do not take advantage of this for security, either because monitoring is still treated as a separate add-on or because there is no simple, working example of how to build detection and automatic response directly into the controller. There is therefore a clear and practical need for a system that uses the central intelligence of SDN to continuously watch traffic across the entire network, automatically recognise the signs of common attacks, and respond immediately and without human intervention. This project sets out to design, build, and test exactly such a system."),

    h2("1.4 Research Questions"),
    p("To give the project a clear direction, it is guided by the following questions:"),
    num("How can the traffic statistics that OpenFlow switches already collect be gathered through an SDN controller and used to monitor a whole network in real time?"),
    num("What patterns in these statistics reliably distinguish common attacks such as port scans, floods or denial-of-service, and abnormal traffic volumes from ordinary, harmless traffic?"),
    num("How can a detection engine be designed so that its alerts are explainable and its sensitivity can be tuned, rather than being an opaque black box?"),
    num("Can the SDN controller not only detect an attack but also respond to it automatically, for example by blocking the attacker, and how quickly and reliably can it do so?"),
    num("How effective is the resulting system at correctly identifying attacks while avoiding false alarms on normal traffic, when tested in a realistic simulated network?"),

    h2("1.5 Aim and Objectives"),
    h3("1.5.1 Aim of the Study"),
    p("The aim of this study is to design, build, and evaluate a traffic-monitoring and anomaly-detection system that runs inside an SDN controller and uses the network-wide view provided by Software-Defined Networking to detect and automatically respond to common network attacks in real time."),
    h3("1.5.2 Objectives of the Study"),
    p("To achieve this aim, the project pursues the following specific objectives:"),
    num("To review the relevant literature on Software-Defined Networking, the OpenFlow protocol, SDN controllers, and network anomaly detection, in order to build a solid foundation for the design."),
    num("To design an SDN controller application that continuously collects flow and port statistics from the switches it manages and turns them into meaningful per-second traffic rates."),
    num("To design and implement an explainable anomaly-detection engine that recognises port scans, floods or denial-of-service attacks, and abnormal traffic volumes, with thresholds that can be tuned."),
    num("To add an automatic mitigation capability that installs temporary blocking rules on the switches when a serious attack is detected."),
    num("To provide a live dashboard and a simple interface so that an administrator can see flows, alerts, and mitigations as they happen."),
    num("To build a realistic virtual test network using Mininet and to generate normal traffic and simulated attacks against it."),
    num("To evaluate the system by measuring how accurately it detects attacks, how well it avoids false alarms, and how quickly it responds."),

    h2("1.6 Scope and Limitations of the Study"),
    h3("1.6.1 Scope of the Study"),
    p("This project focuses on building a working, end-to-end example of SDN-based traffic monitoring and anomaly detection, suitable for a campus or enterprise-style network. Its scope includes the SDN controller application, the detection engine, the automatic mitigation, the monitoring dashboard, and a simulated test network. The system detects three broad and common classes of attack, port scans, floods or denial-of-service, and abnormal traffic volumes, which between them cover a large share of the everyday threats that networks face. The system is built on the Ryu SDN framework using the OpenFlow 1.3 protocol, and is tested on a virtual network created with Mininet and Open vSwitch."),
    p("The scope deliberately covers the whole path from raw switch statistics to a visible alert and an automatic response, so that the project demonstrates a complete and practical solution rather than an isolated piece of theory."),
    h3("1.6.2 Limitations of the Study"),
    p("Like any project carried out within limited time and resources, this work has boundaries that should be stated honestly:"),
    bullet("The system is tested in a simulated network built with Mininet rather than on large-scale physical hardware. Mininet produces realistic behaviour and is widely used in SDN research, but a production network with heavy real-world traffic could reveal issues of scale that a simulation does not."),
    bullet("The detection engine uses statistical thresholds rather than machine learning. This choice keeps the system lightweight, fast, and easy to understand, but a machine-learning approach might detect more subtle or previously unseen attacks, at the cost of greater complexity and the need for training data."),
    bullet("The system focuses on three common classes of attack. It is not intended to be a complete replacement for a full commercial intrusion-detection system, which might recognise a much wider range of specific threats."),
    bullet("The automatic mitigation blocks an attacker by source address for a fixed period. A determined attacker who can change addresses could work around this, so mitigation is best seen as a fast first line of defence rather than a perfect one."),
    bullet("Encrypted traffic is treated the same as any other traffic for the purpose of counting packets and bytes; the system does not inspect the contents of packets, which is both a limitation and, from a privacy standpoint, a deliberate design choice."),

    h2("1.7 Research Methodology (Brief)"),
    p("The project was carried out using the prototyping methodology, in which a working version of the system is built early and then improved through repeated cycles of testing and refinement. This approach was chosen because the project brings together several moving parts, an SDN controller, the OpenFlow protocol, a detection engine, a virtual network, and traffic-generation tools, whose behaviour together is hard to predict fully in advance. Building a prototype and watching it run made it possible to discover and fix problems that would not have been obvious from planning alone. The methodology is described in full in Chapter Three. In brief, the work moved through the stages of reviewing the literature and gathering requirements, designing the system and its detection logic, implementing the controller and supporting tools, and finally testing and evaluating the whole system against normal traffic and simulated attacks."),

    h2("1.8 Organisation of the Project"),
    p("The rest of this report is organised into three further chapters:"),
    bullet("Chapter Two: Literature Review surveys the existing knowledge and tools that this project builds on, including Software-Defined Networking and its architecture, the OpenFlow protocol, the main SDN controllers, and the field of network anomaly detection. It also reviews several similar systems, noting their strengths and weaknesses and identifying the gap this project fills."),
    bullet("Chapter Three: System Analysis and Design explains the methodology used, sets out the functional and non-functional requirements, and presents the design of the system in detail. This includes the input and output design, the way data is stored, and a set of diagrams, architecture, network topology, use case, sequence, class, flowchart, and component, together with pseudocode for the main processes and the hardware and software requirements."),
    bullet("Chapter Four: Implementation and Evaluation describes how the system was actually built, walks through the working code, and presents the testing carried out. It shows the results of running normal traffic and simulated attacks through the system and analyses how well the system performed against its objectives."),
    p("The report ends with a list of references in APA style and a set of appendices containing the full source code of the system."),
    pageBreak(),
  ];
};
